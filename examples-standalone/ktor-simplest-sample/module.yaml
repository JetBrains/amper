product: jvm/app

settings:
  ktor: enabled
  jvm:
    mainClass: com.example.com.ApplicationKt

dependencies:
  - $ktor.server.core
  - $ktor.server.netty
  - $ktor.server.configYaml
  - $logback.classic

test-dependencies:
  - $ktor.server.testHost
