[module]
target = multiplatform
type = library

[multiplatform]
platforms = [jvm, native, ios]

# Kotlin-test should be available by default in test source set
[jvm.test]
framework = junit5 # or junit for (junit4) or testNG
