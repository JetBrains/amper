[module]
groupId = "my.example"
version = "1.0.0"

[kotlin.compilerPlugins]
"org.jetbrains.compose.compiler:compiler" = "42"

[target.android]
minSdkVersion = 42
targetSdkVersion = 42
manifest = "manifest.xml"

# And idea of template application
target.android = "defaultAndroid"
# or
[target.android]
fromTemplate = "defaultAdroid"

[target.android.kotlin]
freeCompilerArgs = [ "--optIn=hello" ]

[target.android.dependencies]
# inter project
model = "local"
shared-ui-elements = "local"