catalogs:
  versions: ../Versions.yaml
  deps: ../Deps.yaml

type: app
platforms: [ js ]

templates:
  - github.com/jetbrains/compose:
      features: [ webCore, runtime ]


dependencies:
  - ../common
  - com.github.chihsuanwu:google-maps-compose-web:0.3.0-alpha

settings:
  webpackDevServer:
    version: ${versions.webpackDevServer}
  webpackCli:
    version: ${versions.webpackCli}