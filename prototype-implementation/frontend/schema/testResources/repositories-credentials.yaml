product:
  type: app
  platforms: [ iosArm64 ]

repositories:
  - id: x
    url: https://x.y
    credentials:
      file: ./repositories-credentials.local.properties
      usernameKey: username.key
      passwordKey: password.key
