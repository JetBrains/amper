type: app
platforms:
  - jvm
jvm:
  kotlin:
    languageVersion: 1.8
    apiVersion: 1.7
    progressiveMode: true
    languageFeatures:
      - InlineClasses
    optIns:
      - kotlin.ExperimentalUnsignedTypes
  dependencies:
    - org.yaml:snakeyaml:2.0

### GOLD
# Potato kotlin_fragment_parts
# Type APPLICATION
# Created programmatically
# Fragments:
# - jvm
#   Fragment dependencies: []
#   External dependencies: [MavenDependency(coordinates=org.yaml:snakeyaml:2.0)]
#   Parts: [KotlinFragmentPart(languageVersion=1.8, apiVersion=1.7, progressiveMode=true, languageFeatures=[InlineClasses], optIns=[kotlin.ExperimentalUnsignedTypes])]
#   Platforms: [JVM]
#   Platforms: [JVM]
# - jvmTest
#   Fragment dependencies: [jvm [FRIEND]]
#   External dependencies: []
#   Platforms: [JVM]
# Artifacts:
# - Built from [jvm]
#   For platforms [JVM]
