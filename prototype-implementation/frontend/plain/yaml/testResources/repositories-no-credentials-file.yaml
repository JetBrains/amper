product:
  type: app
  platforms: [ iosArm64 ]

repositories:
  - id: x
    url: https://x.y
    credentials:
      file: ./non.existing.file
      usernameKey: non.existing.key
      passwordKey: non.existing.key
