product:
  type: app
  platforms: [ iosArm64 ]

repositories:
  - id: x
    url: https://x.y
    credentials:
      # <![Error] Credentials file /non.existing.file does not exist. Error context: when applying the template at /build.yaml!>
      file: ./non.existing.file
      usernameKey: non.existing.key
      passwordKey: non.existing.key
