product:
  type: lib
  platforms: [jvm]

apply:
  - ../../common.module-template.yaml

dependencies:
  - ../android-integration-core: exported
  - org.gradle:gradle-tooling-api:8.5

settings:
  kotlin:
    serialization: json
