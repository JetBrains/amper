product: android/app

dependencies:
  - $compose.foundation
  - $compose.material3
  - androidx.activity:activity-compose:1.7.2 # integration compose with activities

settings:
  android:
    signing: enabled
  compose: enabled
