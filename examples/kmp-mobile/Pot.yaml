product:
  type: app
  platforms: [ iosArm64, android ]

dependencies:
  - org.jetbrains.kotlinx:kotlinx-datetime:0.4.0

dependencies@android:
# list of dependencies for android

dependencies@ios:
# list of dependencies for ios

test-dependencies:
  - org.jetbrains.kotlin:kotlin-test:1.9.0

settings:
  android:
    compileSdkVersion: android-31