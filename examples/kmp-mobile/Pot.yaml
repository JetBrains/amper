product:
  type: app
  platforms: [iosArm64, android]

dependencies:
  - org.jetbrains.kotlinx:kotlinx-datetime:0.4.0

dependencies@android:
  # list of dependencies for android

dependencies@iosArm64:
  # list of dependencies for android

test-dependencies:
  - org.jetbrains.kotlin:kotlin-test:1.8.10

settings:
  kotlin:
    languageVersion: 1.8
