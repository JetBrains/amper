product: jvm/app

apply:
  - ../common.module-template.yaml
  - ../module.module-template.yaml

# Add dependencies
dependencies:
  - org.jetbrains.kotlinx:kotlinx-datetime:0.4.0

# Override templated settings
settings:
  kotlin:
    debug: true
