product: android/app

settings:
  compose: enabled
  kotlin:
    # Kept only temporarily for mobile tests because they don't run from sources and still expect this
    # TODO remove after Amper bump or when mobile tests run from sources
    parcelize: enabled
  android:
    namespace: com.jetbrains.sample.app
    parcelize: enabled


dependencies:
  - com.google.guava:guava:33.0.0-android
  - androidx.appcompat:appcompat:1.6.1
  - androidx.activity:activity-compose:1.7.2
  - $compose.foundation
  - $compose.material3
