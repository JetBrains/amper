product: jvm/lib

apply:
  - ../common.module-template.yaml
  - ../used-in-idea.module-template.yaml # the IDE needs to provision JDKs to set them in the workspace model

dependencies:
  - ../amper-problem-reporting: exported
  - ../core
  - ../frontend-api: exported
  - ../libraries/concurrency
  - ../libraries/foojay-kotlin-client
  - ../libraries/telemetry
  - $libs.junit.jupiter.params
  - $libs.ktor.client.core
  - $libs.ktor.client.logging
  - $libs.opentelemetry.ktor3

test-dependencies:
  - ../test-base
  - $libs.kotlinx.coroutines.debug
