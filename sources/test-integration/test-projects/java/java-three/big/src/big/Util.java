package big;

/**
 * We keep this class to make sure that JIC builds incrementally: otherwise, if more than 85% of files change,
 * then the whole project will be rebuilt without incrementality.
 */
public class Util {
}