product:
  type: jvm/amper-plugin

pluginInfo:
  id: build-konfig
  description: |
    Generates the Kotlin object with all the supplied properties (String)
  settingsClass: org.jetbrains.amper.plugins.buildkonfig.Schema1

plugins:
  hello:
    enabled: true

dependencies:
  - com.squareup:kotlinpoet:2.2.0
