product:
  type: lib
  platforms: [jvm]

apply:
  - ../../common.module-template.yaml

settings:
  kotlin:
    serialization: json

dependencies:
  - ../amper-extensibility-api
  - ../amper-extensibility-schema-model
  - $libs.ksp.symbolProcessingApi
