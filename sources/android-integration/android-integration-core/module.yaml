product: 
  type: lib
  platforms: [jvm]

# This is redundant declaration, but without it build fails
repositories:
  - https://cache-redirector.jetbrains.com/www.jetbrains.com/intellij-repository/releases
  - https://cache-redirector.jetbrains.com/packages.jetbrains.team/maven/p/ij/intellij-dependencies

dependencies:
  - ../../frontend-api

settings:
  kotlin:
    serialization: json
  publishing:
    group: org.jetbrains.amper.android.integration