product: 
  type: lib
  platforms: [jvm]

apply:
  - ../../common.module-template.yaml

dependencies:
  - ../../frontend-api

settings:
  kotlin:
    serialization: json
  publishing:
    group: org.jetbrains.amper.android.integration
