product: jvm/lib

apply:
  - ../common.module-template.yaml

settings:
  ktor: enabled
  kotlin:
    serialization: json
  publishing:
    enabled: true

dependencies:
  - ../libraries/async-processes

  - $ktor.client.core
  - $ktor.client.cio
  - $ktor.client.contentNegotiation
  - $ktor.serialization.kotlinx.json

  - $libs.hot.reload.devtools.api
  - $libs.kotlinx.coroutines.core
