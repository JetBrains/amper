type: jvm/external-task

module: ../../build-sources/upload-dist

programArguments:
  - --output-dir=${outputDirectory}
  - --classpath=${module(.).runtimeClasspathJvm}
  - --extra-dir=plugins-processor=${module(../extensibility/amper-schema-processor).runtimeClasspathJvm}
  - --extra-dir=extensibility-api=${module(../extensibility/amper-extensibility-api).runtimeClasspathJvm}
  - --repository=maven-local
