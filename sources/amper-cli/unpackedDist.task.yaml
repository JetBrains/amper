type: jvm/external-task

module: ../../build-sources/build-unpacked-dist

programArguments:
  - --output-dir=${outputDirectory}
  - --target-dir=${outputDirectory}/dist
  - --classpath=${module(.).runtimeClasspathJvm}
  - --extra-dir=plugins-processor=${module(../extensibility/amper-schema-instrumentator).runtimeClasspathJvm}
  - --extra-dir=extensibility-api=${module(../extensibility/amper-extensibility-api).runtimeClasspathJvm}