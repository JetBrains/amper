product: jvm/lib

description: Contains template definitions that can be used to generate Amper projects (e.g. in `./amper init`).

apply:
  - ../common.module-template.yaml
  - ../used-in-idea.module-template.yaml

dependencies:
  - ./amper-project-template-resources

plugins:
  generate-template-resources: enabled

settings:
  kotlin:
    serialization: json
