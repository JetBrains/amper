product: jvm/lib

apply:
  - ../common.module-template.yaml
  - ../used-in-idea.module-template.yaml

dependencies:
  - ./amper-project-template-resources

settings:
  kotlin:
    serialization: json
