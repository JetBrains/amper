product: jvm/lib

apply:
  - ../common.module-template.yaml
  - ../used-in-idea.module-template.yaml

dependencies:
  - ./amper-project-template-resources

settings:
  generate-template-resources: enabled
  kotlin:
    serialization: json
