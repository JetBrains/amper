product: jvm/lib

description: Utilities for running processes with a coroutines-friendly API.

apply:
  - ../../common.module-template.yaml

dependencies:
  - $libs.kotlinx.coroutines.core

settings:
  publishing:
    enabled: true
