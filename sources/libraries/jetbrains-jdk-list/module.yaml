product: jvm/lib

description: Library to download JDK metadata from the JetBrains JSON registry.

apply:
  - ../../common.module-template.yaml
  - ../../used-in-idea.module-template.yaml

dependencies:
  - $libs.ktor.client.core
  - $libs.xz

test-dependencies:
  - $libs.ktor.client.cio

settings:
  kotlin:
    serialization: json
