product: jvm/lib

description: PSI support for TOML files, copied from IntelliJ.

apply:
  - ../../../common.module-template.yaml
  - ../../../used-in-idea.module-template.yaml

dependencies:
  - ../parserutil-stub
