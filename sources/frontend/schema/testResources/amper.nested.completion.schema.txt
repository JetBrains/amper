module (isRegExp=false,isolated=false)
plugin (isRegExp=false,isolated=false)
product (isRegExp=false,isolated=false)
settings (isRegExp=false,isolated=false)
  android (isRegExp=false,isolated=false)
    parcelize (isRegExp=false,isolated=false)
    resourcePackaging (isRegExp=false,isolated=false)
    signing (isRegExp=false,isolated=false)
  compose (isRegExp=false,isolated=false)
    experimental (isRegExp=false,isolated=false)
      hotReload (isRegExp=false,isolated=false)
    resources (isRegExp=false,isolated=false)
  ios (isRegExp=false,isolated=false)
    framework (isRegExp=false,isolated=false)
  java (isRegExp=false,isolated=false)
    annotationProcessing (isRegExp=false,isolated=false)
  jvm (isRegExp=false,isolated=false)
    test (isRegExp=false,isolated=false)
  kotlin (isRegExp=false,isolated=false)
    allOpen (isRegExp=false,isolated=false)
    ksp (isRegExp=false,isolated=false)
    noArg (isRegExp=false,isolated=false)
    powerAssert (isRegExp=false,isolated=false)
    serialization (isRegExp=false,isolated=false)
  kover (isRegExp=false,isolated=false)
    html (isRegExp=false,isolated=false)
    xml (isRegExp=false,isolated=false)
  ktor (isRegExp=false,isolated=false)
  lombok (isRegExp=false,isolated=false)
  native (isRegExp=false,isolated=false)
  publishing (isRegExp=false,isolated=false)
  springBoot (isRegExp=false,isolated=false)
settings(@[A-z]+)? (isRegExp=true,isolated=true)
  android (isRegExp=false,isolated=false)
    parcelize (isRegExp=false,isolated=false)
    resourcePackaging (isRegExp=false,isolated=false)
    signing (isRegExp=false,isolated=false)
  compose (isRegExp=false,isolated=false)
    experimental (isRegExp=false,isolated=false)
      hotReload (isRegExp=false,isolated=false)
    resources (isRegExp=false,isolated=false)
  ios (isRegExp=false,isolated=false)
    framework (isRegExp=false,isolated=false)
  java (isRegExp=false,isolated=false)
    annotationProcessing (isRegExp=false,isolated=false)
  jvm (isRegExp=false,isolated=false)
    test (isRegExp=false,isolated=false)
  kotlin (isRegExp=false,isolated=false)
    allOpen (isRegExp=false,isolated=false)
    ksp (isRegExp=false,isolated=false)
    noArg (isRegExp=false,isolated=false)
    powerAssert (isRegExp=false,isolated=false)
    serialization (isRegExp=false,isolated=false)
  kover (isRegExp=false,isolated=false)
    html (isRegExp=false,isolated=false)
    xml (isRegExp=false,isolated=false)
  ktor (isRegExp=false,isolated=false)
  lombok (isRegExp=false,isolated=false)
  native (isRegExp=false,isolated=false)
  publishing (isRegExp=false,isolated=false)
  springBoot (isRegExp=false,isolated=false)
test-settings (isRegExp=false,isolated=false)
  android (isRegExp=false,isolated=false)
    parcelize (isRegExp=false,isolated=false)
    resourcePackaging (isRegExp=false,isolated=false)
    signing (isRegExp=false,isolated=false)
  compose (isRegExp=false,isolated=false)
    experimental (isRegExp=false,isolated=false)
      hotReload (isRegExp=false,isolated=false)
    resources (isRegExp=false,isolated=false)
  ios (isRegExp=false,isolated=false)
    framework (isRegExp=false,isolated=false)
  java (isRegExp=false,isolated=false)
    annotationProcessing (isRegExp=false,isolated=false)
  jvm (isRegExp=false,isolated=false)
    test (isRegExp=false,isolated=false)
  kotlin (isRegExp=false,isolated=false)
    allOpen (isRegExp=false,isolated=false)
    ksp (isRegExp=false,isolated=false)
    noArg (isRegExp=false,isolated=false)
    powerAssert (isRegExp=false,isolated=false)
    serialization (isRegExp=false,isolated=false)
  kover (isRegExp=false,isolated=false)
    html (isRegExp=false,isolated=false)
    xml (isRegExp=false,isolated=false)
  ktor (isRegExp=false,isolated=false)
  lombok (isRegExp=false,isolated=false)
  native (isRegExp=false,isolated=false)
  publishing (isRegExp=false,isolated=false)
  springBoot (isRegExp=false,isolated=false)
test-settings(@[A-z]+)? (isRegExp=true,isolated=true)
  android (isRegExp=false,isolated=false)
    parcelize (isRegExp=false,isolated=false)
    resourcePackaging (isRegExp=false,isolated=false)
    signing (isRegExp=false,isolated=false)
  compose (isRegExp=false,isolated=false)
    experimental (isRegExp=false,isolated=false)
      hotReload (isRegExp=false,isolated=false)
    resources (isRegExp=false,isolated=false)
  ios (isRegExp=false,isolated=false)
    framework (isRegExp=false,isolated=false)
  java (isRegExp=false,isolated=false)
    annotationProcessing (isRegExp=false,isolated=false)
  jvm (isRegExp=false,isolated=false)
    test (isRegExp=false,isolated=false)
  kotlin (isRegExp=false,isolated=false)
    allOpen (isRegExp=false,isolated=false)
    ksp (isRegExp=false,isolated=false)
    noArg (isRegExp=false,isolated=false)
    powerAssert (isRegExp=false,isolated=false)
    serialization (isRegExp=false,isolated=false)
  kover (isRegExp=false,isolated=false)
    html (isRegExp=false,isolated=false)
    xml (isRegExp=false,isolated=false)
  ktor (isRegExp=false,isolated=false)
  lombok (isRegExp=false,isolated=false)
  native (isRegExp=false,isolated=false)
  publishing (isRegExp=false,isolated=false)
  springBoot (isRegExp=false,isolated=false)