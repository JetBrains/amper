product:
  type: lib
  platforms: [ iosArm64 ]

repositories:
  - id: x
    url: https://x.y
    credentials:
      file: ./repositories-credentials.local.properties
      usernameKey: username.key
      passwordKey: <![Error] Credentials file {{ testResources }}{{ fileSeparator }}repositories-credentials.local.properties does not have the key [non.existing.key]. Available keys are: [username.key, password.key]!>non.existing.key<!>