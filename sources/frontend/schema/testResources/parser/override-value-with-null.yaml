product: jvm/app

apply:
  - ./override-value-with-null.module-template.yaml

settings:
  kotlin:
    languageVersion: null # override the template value which itself overrides the null default
  jvm:
    release: null # override the non-null default
