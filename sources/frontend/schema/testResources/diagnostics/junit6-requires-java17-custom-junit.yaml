product: jvm/lib

settings:
  jvm:
    jdk:
      version: <![Error] JUnit Platform version 6.0.0 requires at least JDK 17, but this module uses JDK 13.!>13<!>
    test:
      junitPlatformVersion: <![Error] JUnit Platform version 6.0.0 requires at least JDK 17, but this module uses JDK 13.!>6.0.0<!>