product:
  type: lib
  platforms: [ iosArm64, iosX64, iosSimulatorArm64, android ]

aliases:
  myAlias: [ <![Error] Alias myAlias uses non-leaf platform ios, which is not supported at the moment!>ios<!>, android ]