product:
  type: lib
  platforms:
    - jvm

dependencies:
  - <![Error] Module dependencies loop detected: `lib-c -> lib-a -> lib-b -> lib-c`!>../lib-c<!>
  - <![Error] Module lib-b can not depend on itself!>../lib-b<!>