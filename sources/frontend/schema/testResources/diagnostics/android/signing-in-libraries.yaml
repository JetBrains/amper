product:
  type: lib
  platforms: [jvm, android]

settings:
  android:
    <![Warning] Setting 'signing' cannot be applied to product type 'lib'. Supported product types are: android/app!>signing<!>: enabled