product:
  type: lib
  platforms: [jvm, android]

settings:
  android:
    <![Warning] Android signing enabled, but testResources{{ fileSeparator }}diagnostics{{ fileSeparator }}android{{ fileSeparator }}keystore.properties does not exist!>signing: enabled<!>