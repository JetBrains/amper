product:
  type: lib
  platforms: [jvm, android]

settings:
  android:
    <![Warning] Android signing enabled, but testResources/diagnostics/android/keystore.properties does not exist!>signing: enabled<!>