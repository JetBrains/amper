product:
  type: lib
  platforms: [ jvm ]

settings:
  jvm:
    <![Warning] Module lib-b uses JVM release 17 (default) but depends on module lib-c with a higher JVM release 24!><![Warning] Module lib-a uses JVM release 11 but depends on module lib-c with a higher JVM release 24!>release: 24<!><!>