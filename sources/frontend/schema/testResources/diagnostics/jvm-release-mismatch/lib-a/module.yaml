product:
  type: lib
  platforms: [ jvm ]

dependencies:
  - <![Warning] Module lib-a uses JVM release 11 but depends on module lib-b with a higher JVM release 17 (default)!>../lib-b<!>
  - <![Warning] Module lib-a uses JVM release 11 but depends on module lib-c with a higher JVM release 24!>../lib-c<!>

settings:
  jvm:
    <![Warning] Module lib-a uses JVM release 11 but depends on module lib-b with a higher JVM release 17 (default)!><![Warning] Module lib-a uses JVM release 11 but depends on module lib-c with a higher JVM release 24!>release: 11<!><!>