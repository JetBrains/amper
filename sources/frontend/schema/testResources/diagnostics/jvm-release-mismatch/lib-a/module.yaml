product:
  type: lib
  platforms: [ android, jvm, iosArm64, iosSimulatorArm64, iosX64, linuxArm64, linuxX64, macosArm64, macosX64, mingwX64 ]

dependencies:
  - <![Warning] Module lib-a uses JVM release 11 but depends on module lib-b with a higher JVM release 21 (default)!>../lib-b<!>
  - <![Warning] Module lib-a uses JVM release 11 but depends on module lib-c with a higher JVM release 24!>../lib-c<!>

settings:
  jvm:
    release: <![Warning] Module lib-a uses JVM release 11 but depends on module lib-b with a higher JVM release 21 (default)!><![Warning] Module lib-a uses JVM release 11 but depends on module lib-c with a higher JVM release 24!>11<!><!>