product: jvm/app

# Direct dependencies on other modules.
# Corresponding test case checks that transitive dependencies are collected correctly
dependencies:
  - com.intellij.platform:kotlinx-coroutines-debug:1.10.1-intellij-4

repositories:
  - https://cache-redirector.jetbrains.com/packages.jetbrains.team/maven/p/ij/intellij-dependencies

settings:
  internal:
    excludeDependencies:
      - com.intellij.platform:kotlinx-coroutines-core-jvm
