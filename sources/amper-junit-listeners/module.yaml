product:
  type: lib
  platforms: [ jvm ]

apply:
  - ../common.module-template.yaml

dependencies:
  - $libs.junit.platform.launcher: compile-only
  - $libs.mordant
  - $libs.teamcity.serviceMessages
