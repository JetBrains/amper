product:
  type: lib
  platforms: [ jvm ]

test-dependencies:
  - ../test-base
  - ../cli
  - ../core
  - $libs.charleskorn.kaml
  - $libs.mordant

apply:
  - ../common.module-template.yaml

settings:
  kotlin:
    serialization: enabled
