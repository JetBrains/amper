inherit: ../module.yaml

platform:
  - type: jvm
    version: 1.8
    compiler-settings:
      runtime-logs:
        - type: gc
          level: debug

# Seems inconsistent with
# dependencies:
#  - common:
#    - core:local

dependencies:
  - core:local
  - model-serialization:local