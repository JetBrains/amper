# Seems that this field wil be inherited to submodules?
my-app-version: 1.3.0

version: {.my-app-version}

compose:
  version: 1.4.0-rc01

kotlin:
  compiler-settings:
    plugins:
      - org.jetbrains.compose.compiler:compiler:{.compose.version}
  compatibility:
    api-version: 1.8
    language-version: 1.8