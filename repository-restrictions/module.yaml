product: jvm/lib

description: Contains tests to enforce rules in the Amper project itself.

apply:
  - ../sources/common.module-template.yaml

test-dependencies:
  - ../sources/test-base
  - ../sources/core
  - ../sources/frontend/schema
  - $libs.charleskorn.kaml

settings:
  kotlin:
    serialization: enabled
