# in order for lib to be published or used, it must have coordinates.
# todo see QUESTIONS.md#Coordinates -- regarding local dependenceis
id="org.my.my-lib:0.1"
version="0.1"
# or
# groupId="org.my"
# artifactId="my-lib"
# todo see QUESTIONS.md#Coordinates -- regarding schema

[[target]]
type = "library"
# platform="multiplatform" # By default we produce an mpp library, so we need to explicitly declare it

[dependencies]
# Depend on multi-platform library. Tool will figure out platform-specific version
"kotlin:kotlin-logging"="1.2" # by default we use koltin/java distribution (Maven central)
