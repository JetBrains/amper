# Since there is no kotlin files, only Java toolchain will be enabled

#[java] default settings for the java tool
# todo see QUESTIONS.md#Toolchain
#jdk=17   # if not specified, the build tool will take the latest and put it  'lock' file.
#sourceVersion=13
#targetVersion=17

# By default we create a single JVM target # todo see QUESTIONS.md#Defaults
# [target]
# platform = "jvm"
# type = "binary" # By default we create a binary target # todo see QUESTIONS.md#Defaults

[dependencies]
"log4j:log4j-common"="1.2" # by default we use koltin/java distribution (Maven central)


